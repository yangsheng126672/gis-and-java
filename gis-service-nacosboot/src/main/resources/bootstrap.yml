spring:
  application:
    name: gis-service-app
  cloud:
    nacos:
      config:
        server-addr: ms-cc-00:8848
        file-extension: properties
        group: sw-biz
  profiles:
    active: test